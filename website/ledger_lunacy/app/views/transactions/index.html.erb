<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Historical Transactions</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container pt-5">
<h1>Investing Account</h1>
  </div>
  <div class="balance-bg py-4 shadow-sm mt-4">
    <div class="container">
        <div class="row">
            <div class="col-md-3 rounded-3 bg-success balance-box p-3">
                <h2 class="text-white" id="total-balance">
                    $<span id="balance-value">0</span>
                </h2>
                <span class="text-warning fs-3 text-uppercase">Balance</span>
            </div>
        </div>
    </div>
  </div>
  <div class="container mt-5 pb-5">
    <h2 class="mt-5 mb-4">Past Transactions</h2>
    <div class="table-responsive">
      <table class="table table-hover">
        <thead>
          <tr>
            <th scope="col" class="p-3">Date</th>
            <th scope="col" class="p-3">Transaction</th>
            <th scope="col" class="p-3"></th>
            <th scope="col" class="p-3">Amount</th>
            <th scope="col" class="p-3">Balance</th>
          </tr>
        </thead>
        <tbody>
          <% @transactions.each do |transaction| %>
          <% unique_transaction = find_unique_transaction(transaction) %>
            <tr>
              <td class="p-3"><%= unique_transaction.date.strftime("%d/%m/%Y") %></td>
              <td class="p-3"><%= unique_transaction.requester_type %></td>
              <td class="p-3"><%= unique_transaction.destination_description %></td>
              <td class="p-3"><%= unique_transaction.amount %></td>
              <td class="p-3 balance-value"><%= unique_transaction.balance %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const firstBalanceValue = document.querySelector('.balance-value').textContent;
    document.getElementById('balance-value').textContent = firstBalanceValue;
  </script>
</body>
</html>
